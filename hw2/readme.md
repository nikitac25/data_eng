1. Create in hw2 directory .env file wit the following variables:
   MYSQL_USER=yourusername
   MYSQL_PASSWORD=yourpassword
   MYSQL_DATABASE=assessment_db
   MYSQL_HOST=host.docker.internal
   
2.Run the following commands to set up MySQL and load the data:
  bash start_mysql.sh
  bash insert_data.sh
  bash create_csv.sh
