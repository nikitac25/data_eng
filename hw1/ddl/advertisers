/*
Advertisers were isolated to separate table in case in future will be retrieved more data about advertisers and table will be extended
*/

CREATE TABLE advertisers (
    id INT PRIMARY KEY,
    name VARCHAR(100)
);
